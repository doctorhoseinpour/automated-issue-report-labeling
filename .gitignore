# Datasets
*.csv
*.tsv
*.json
*.jsonl

# Exception: analysis output CSVs are paper artifacts (small, derived from results/)
!docs/analysis/*.csv

# Exception: the source dataset pool — does not regenerate, needed to bootstrap
# a fresh clone. The train/test splits stay ignored; they are deterministic.
!issues11k.csv

# Results
results/

# Python virtual environment
venv/
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
.eggs/

# HuggingFace model cache (NRP)
hf_cache/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# FAISS cache
.faiss_cache/

# Unsloth
unsloth_compiled_cache/

# Misc
.env
*.log

# NRP sync staging directory
fetched/

# Archived data (OOM runs, 1-epoch FT backup) — preserved on disk, not in git
archive/

# OpenAI RAGTAG canary outputs — disposable, never archival
canary_openai/
venv-setfit/

# Small per-cell metric CSVs that the SANER 2027 figure/table generators read (scripts/paper/*.py)
!paper/tables/*.csv
