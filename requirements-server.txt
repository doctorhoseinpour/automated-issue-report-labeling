# Portable requirements for remote server (A100/A6000).
#
# Setup:
#   python3 -m venv venv && source venv/bin/activate
#   pip install --upgrade pip
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements-server.txt
#
# unsloth handles its own transformers/tokenizers versions — do not pin them here.

# LLM & fine-tuning (unsloth pulls transformers, tokenizers, etc.)
unsloth
unsloth_zoo
trl
peft
accelerate
bitsandbytes
datasets

# FAISS retrieval
faiss-cpu

# Embeddings
sentence-transformers
langchain-community
langchain-core

# Data & evaluation
pandas
numpy
scikit-learn

# Utilities
psutil
tqdm
PyYAML
safetensors
